# 사용방법 요약

## 처음 한 번만
1. 텔레그램 설치 → 검색창에 `BotFather` → `/newbot`으로 봇 만들기 → **토큰** 복사
2. 만든 봇을 검색해서 들어가 **시작(START)** 누르고 "안녕" 보내기
3. 폴더에서 PowerShell을 열어 `pip install -r requirements.txt` 한 번 실행
4. (선택) 텔레그램만 먼저 따로 연결하려면 **`setup_telegram.bat` 더블클릭** — 마우스 영역 지정 없이 이 단계만 함

## 준비 작업 → `setup_capture.bat` 더블클릭
1. 크롬에 감시할 버튼 4개가 **전부 "매진"으로 보이는 예매 화면**을 열어 둡니다.
2. 텔레그램 질문에 `y` → 토큰 붙여넣기 → (봇에 메시지 보낸 뒤) Enter
   (이미 연결했다면 `n`)
3. 검은 창을 지정할 영역을 가리지 않는 구석에 두고, 안내에 따라 마우스로 6곳을 지정합니다.
   각 영역마다 **왼쪽 위 모서리 → 오른쪽 아래 모서리** 순서로 6초 안에 마우스를 올려둡니다.

| 번호 | 영역 | 용도 |
|---|---|---|
| 1 | 기준 칸 (출발·도착 시간 글자) | 화면이 정상인지 확인 (그림 저장) |
| 2~5 | 버튼 1~4번의 '매진' 표시 칸 | 매진 여부 판단 (버튼마다 따로 그림 저장, 크기가 달라도 됨). 동시에 여러 개 풀리면 **앞 번호가 우선 클릭**됨 |
| 6 | 예약 버튼 영역 | 버튼 클릭 뒤 이어서 무작위 한 점 클릭 |

4. 6번 지정이 끝나면 **5초 뒤** 캡처됩니다. 그동안 버튼 4개가 전부 매진 상태인지 확인하세요.

## (선택) 예약 후 안내 팝업 자동 확인 → `setup_popup_click.bat` 더블클릭
예약 버튼 누른 뒤 "이용안내" 팝업이 가끔 뜨는 화면이면, 이 팝업의 '확인' 버튼도 자동으로 누르게 설정할 수 있어요.
1. 코레일에서 실제로 예약 버튼을 눌러 그 팝업을 띄워 둡니다.
2. `setup_popup_click.bat` 더블클릭 → 팝업 기준 칸, '확인' 버튼 순서로 2곳 지정 → 5초 뒤 캡처
(안 해도 프로그램은 정상 작동해요. 팝업이 없으면 그냥 건너뜁니다.)

## 연습 → `watcher_dryrun.bat` 더블클릭
F5·클릭·알림 없이 3초마다 판정만 보여 줍니다. 매진 화면에서 `soldout`이 나오면 정상입니다. (Esc로 종료)

## 실제 감시 → `watcher.bat` 더블클릭
1. 시작 후 5초 안에 **크롬 예매 화면을 맨 앞**으로 가져옵니다.
2. F5 → 0.5초마다 화면이 떴는지 확인 → 뜨면 0.9~1.3초 뒤 판단 → 4개 버튼 전부 매진이면 랜덤 대기 → 다시 F5 … 반복
   (F5 후 60초가 지나도 화면이 안 뜨면 텔레그램으로 알리고 멈춤)
3. 매진 아닌 버튼이 뜨면(여러 개면 앞 순서 우선): 그 버튼 클릭 → 예약 버튼 클릭 → (안내 팝업 설정해뒀으면 확인까지) → **텔레그램 알림** → 종료
4. 결제와 예약 확정은 직접 하세요.

## 멈추기
- **Esc 키**
- 마우스를 **화면 모서리**로 세게 밀기

## 시간 바꾸기
`시간설정.json`을 메모장으로 열어 숫자를 고치고 저장 (소수점 가능: `0.5`, 초 단위)
- `wait_seconds`: 매진일 때 다음 F5까지 랜덤 대기 (새로고침 간격, 너무 짧게 하지 마세요)
- 나머지 항목 설명은 `README_시작하기.md`의 "시간 바꾸기" 참고

## 자주 나는 문제
| 증상 | 해결 |
|---|---|
| 매진인데 `available`, 또는 반대로 나옴 | 저장된 그림이 반대 상태입니다(버튼 4개 전부 매진일 때 찍어야 함). **매진 화면에서** `setup_recapture.bat` 더블클릭 |
| `unknown`이 계속 나옴 | 크롬 창 크기·위치·배율이 바뀜, 또는 검은 창이 영역을 가림. 영역이 바뀐 거면 준비 작업 처음부터 다시 |
| 5번 연속 화면 이상 | 자동 중지 + 텔레그램 경고. 로그인·오류창 확인 |
| 크롬이 맨 앞이 아님 | F5를 누르지 않고 기다림. 크롬을 앞으로 |
| 영역은 그대로, 그림만 다시 찍기 | `setup_recapture.bat` 더블클릭 |
| 예약 버튼 영역만 다시 지정 | `setup_reserve_click.bat` 더블클릭 |
| 텔레그램만 따로 (다시) 연결 | `setup_telegram.bat` 더블클릭 |
| 예약 후 "이용안내" 팝업의 확인 버튼도 자동으로 누르고 싶음 | `setup_popup_click.bat` 더블클릭 (선택 기능) |

## 주의
- PC 절전/화면 잠금을 꺼 두세요.
- 크롬 창 크기·위치·화면 배율을 바꾸면 준비 작업을 다시 해야 합니다.
- `config.json`(봇 토큰 포함)은 남에게 보내지 마세요.
- 코레일 약관은 자동 프로그램을 제한할 수 있습니다. 본인 책임으로 사용하세요.
